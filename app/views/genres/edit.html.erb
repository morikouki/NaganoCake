<div class="row">
	<h2>ジャンル編集</h2>
	<%= form_with model: @genre, url: genre_path(@genre) do |f| %>
		<%= f.label :name, 'ジャンル' %>
		<%= f.text_field :name %>
		<%= f.submit '変更を保存する', class: 'btn btn-primary' %>
	<% end %>
</div>